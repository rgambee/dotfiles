# Open any file with a fragment
protocol file
fragment_matches [0-9]+
mime text/*
action launch --type=background cursor ${FILE_PATH}:${FRAGMENT} 

# For text files without fragments
protocol file
mime text/*
action launch --type=background cursor ${FILE_PATH}
