[project]
name = "trusted-tools-uvx"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp-grafana>=0.14.0",
    "postgres-mcp>=0.3.0",
    "ruff>=0.15.17",
]

[tool.uv]
exclude-newer = "7 days"
